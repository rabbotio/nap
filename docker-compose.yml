version: "3"

services:
  next:
    container_name: next
    env_file: .env
    environment:
      - HTTP_PORT=${HTTP_PORT:-3000}
    image: rabbotio/nap
    restart: always
    links:
      - redis
      - mongo
    working_dir: /usr/app
    ports:
      - "9229:9229" # V8 inspector
      - "5858:5858" # Node Inspector port
      - "3000:${HTTP_PORT:-3000}" # HTTP port
    volumes:
      - ${SRC_NEXT:-./.next}:/usr/app/.next
      - ${SRC_MONGOOSE_MODELS:-./models}:/usr/app/models
    build: .
    command: npm run server
    networks:
      - front
      - back

  redis:
    image: redis:3.2.6-alpine
    container_name: redis
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - redis:/data
    networks:
      - back

  mongo_data:  
    image: mongo:3.4
    container_name: mongo_data
    command: bin/true
    networks:
      - back

  mongo:  
    image: mongo:3.4
    container_name: mongo
    volumes:
      - mongo_data:/data/db
    ports:
      - "27017:27017"
    command: --smallfiles --rest
    networks:
      - back

networks:
  front:
  back:

volumes:
  redis:
  mongo_data: