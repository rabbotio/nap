let subscription_memory = {};

const {
  sub,
  subList,
  unSub,
  unSubList,
  pubTopic,
  pubToken,
  getSubList,
  isSub
} = require('./installation');

let sub = (topicString, UID) => {

}

let unsub = (topicString, UID) => {

}

let pubTopic = (topicString, UID) => {

}

let pubUID = (UID, message, data) => {

}

let getSubList = (UID) => {
  
}

let isSub = (topicString, UID) => {
 
}